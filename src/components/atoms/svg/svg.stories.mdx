import { Meta, Canvas, Story, Subtitle } from '@storybook/addon-docs';
import {STORYBOOK} from "../../../constants"
import SVG from './svg';

<Meta title="menu/atoms/svg" />

# SVG

<Subtitle>a svg container</Subtitle>

A blank canvas where we can create data visualizations, games and other experiences with vector graphics.

<Canvas>
  <Story name="basic">
    <SVG />
  </Story>
    <Story name="with text">
    <SVG customStyle={{
        border: '4px solid pink',
      }}>
    <text x={20} y={30} style={{font: "bold 30px sans-serif"}}>hello world</text>
    </SVG>
  </Story>
</Canvas>