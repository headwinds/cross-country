import { Meta, Canvas, Story, Subtitle } from '@storybook/addon-docs';
import { STORYBOOK } from "../../../constants";
import { Hunter } from "../../"
import Stage from './stage';
import styles from "./stage.scss";

<Meta title="menu/organisms/stage" />

# Stage

<Subtitle>All the world's a stage, and all the men and women merely players</Subtitle>

<Canvas>
     <Stage 
          name='stage'
          config={{
            column: { customStyle: { position: 'relative', height: 200, width: 200, backgroundColor: '#eee' } },
          }}
        >
          <Hunter model={{ customStyle: { position: 'absolute', zIndex: 0, left: 20, top: 100 } }} />
        </Stage>
</Canvas>