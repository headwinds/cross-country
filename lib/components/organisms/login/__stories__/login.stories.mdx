import { Meta, Canvas, Story, Subtitle } from '@storybook/addon-docs';
import Login from '../login';

import { Paragraph, Link, Hilight, SubHeadline, Column } from '../../../';
import LoginStory from './login-story';

<Meta title="design system/organisms/login" />

# Login

Until have better Web-based biometrics, we need to include a login within our apps to identify the user. 

<Canvas>
  <Story name="login">
    <SubHeadline text="Simple Version" />
    <LoginStory
      isAnimated={false}
      hasImage={false}
      hasBackground={false}
      imageUrl={null}
    />
  </Story>
  <Story name="login with animations">
    <SubHeadline text="Animated Version" />
    <LoginStory
      isAnimated={true}
      hasImage={false}
      hasBackground={false}
      imageUrl={null}
    />
  </Story>
</Canvas>
