import { Meta, Canvas, Story, Subtitle } from '@storybook/addon-docs';
import Actor from '../actor';

<Meta title="design system/organisms/actors/actor" />

# Actor

This provides the concept of inheritennce where we have a base actor to give all other actors the same elements like a name or health bars

<Canvas>
  <Story name="basic actor">
    <Actor />
  </Story>
  <Story name="positioned actors">
    <Actor position={{ x: 200, y: 200, z: 0 }} customSkinStyle={{ backgroundColor: 'grey' }} />
    <Actor position={{ x: 100, y: 100, z: 0 }} customSkinStyle={{ backgroundColor: 'pink' }} />
    <Actor position={{ x: 300, y: 0, z: 0 }} customSkinStyle={{ backgroundColor: 'orange' }} />
  </Story>
</Canvas>
