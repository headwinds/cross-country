import { Meta, Canvas, Story, Subtitle } from "@storybook/blocks";
import * as BranchesStories from "./branches.stories";

<Meta of={BranchesStories} />

# Branches

This is the RSS Feed Branches component that powers [golds](https://golds.vercel.app).

```jsx
const onLoadedCallback = (error) => {
  console.log(error.message);
};

const feedUrl = "http://localhost:5000/api/porthole/trees";

<Branches onLoadedCallback={onLoadedCallback} feedUrl={feedUrl} />;
```

### Story

<Canvas of={BranchesStories.BranchesStoryWithLocalUrl} />

### Digital Garden

- [Brad Woods](https://garden.bradwoods.io/)
- [paperarrow](https://paperarrow.com/notes/knowledge-garden)
