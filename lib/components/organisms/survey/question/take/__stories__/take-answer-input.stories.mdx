import { Meta, Canvas, Story, Subtitle } from '@storybook/addon-docs';
import MultipleChoiceStory from './multiple-choice-story';
import TakeAnswerInput from './take-answer-input-story';

<Meta title="design system/organisms/survey/question/text input single row" />

# Text Input Question & Answer

`TakeAnswerInput` allows a user to enter a text answer to a question in a single row which is ideal for pasting in a url.

<Canvas>
  <Story name="text input single row">
    <TakeAnswerInput />
  </Story>
</Canvas>