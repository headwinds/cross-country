import { Meta, Canvas, Story, Subtitle } from '@storybook/addon-docs';
import Grid from '../grid';
import styles from '../grid.module.css';

<Meta title="design system/atoms/grid" />

# Grid

Similar to Column or Row, the Grid component is a layout component that allows you to create a layout to contain children in a certain pattern; obviously a grid. 

It is a div container that wraps its children using [css grid](https://css-tricks.com/snippets/css/complete-guide-grid/).

<Canvas>

  <Story name="grid">
    <Grid customStyle={{ width: 120 }}>
      <div><p>1</p></div>
       <div><p>3</p></div>
      <div><p>3</p></div>
      <div><p>4</p></div>
    </Grid>
  </Story>
</Canvas>